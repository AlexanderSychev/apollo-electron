/** Type of event from render process to main process */
export const APOLLO_REQUEST_EVENT_TYPE: string = 'apollo-request';
/** Type of event from main process to render process */
export const APOLLO_RESPONSE_EVENT_TYPE: string = 'apollo-response';
